# 2026-09-24

## P1, minimal, English: request `20260924-165445-869-45b5`

- **Game:** RimWorld 1.6.4871 rev600 on Linux in the WSL, Pickle's own runner, English. Set name in the report:
  `sans-facultatifs`, the minimal set the launcher stages. The suite was discovered whole: 8 features, 20
  scenarios.
- **Revision:** this repository at `ca3b000`. The step DLL was built at 16:44 the same day, after the last edit
  to the steps, and only the two files of the companion under `Tests/Pickle/Mod` and this DLL are staged.
- **Result:** `exitReason: failed`, 20 scenarios, **13 passed, 2 failed, 5 skipped**. The 5 skipped are the ones
  expected: `05` (1) and `08` (4), which need their mods.

| Scenario | Outcome |
|---|---|
| `01` the mod is loaded and its defs exist | passed |
| `01` nothing logged while the defs loaded names one of them | **failed** |
| `01` the mod's own identifier appears in no error or warning | **failed** |
| `01` every species carries the wildness the game loaded | passed |
| `02` each species can be generated and stands on the map | passed |
| `02` the nine together, for a person to look at | passed |
| `03` every species leaves ebbb meat | passed |
| `03` every species leaves ebbb leather | passed |
| `03` the thrumebbb leaves its horn | passed |
| `04` the nine species are still there after a reload | passed |
| `06` the five English label and description scenarios | passed |
| `05`, `08` x4 | skipped by requirement |

- **The two failures are not the mod's.** Both read the game's own log and matched two lines about the test
  companion, whose name, "Ebbbs Renew - Pickle tests", contains "ebbb": a warning that its dependency on this
  mod declares no download URL, and an error that it "did not load any content". The rest of `Player.log` holds no
  error or warning naming a def, a texture, a duplicate or a patch of this mod. The cause is in the harness: the
  steps now leave out lines that name the companion, and the companion's dependency now declares the
  Workshop URL of this mod's item.
- **What passing settled.** The nine species spawn on `test-colony` at the cells taken from other suites; the
  adult age set by the butchering step lands in the last life stage, since the thrumebbb left its horn;
  Pickle's "a X exists" counts an animal; save and reload round-trips the nine; and every one of the 104 English
  texts reads as the inventory says.
- **The capture.** `ebbbs-nine.jpg` was opened at full size before it was minified. Nine distinct shapes stand in
  a row on open ground east of the base, left to right: the small ebbb, the white beee, the ebbbomination, the
  tall thin goliebbb, the drebbbd, the ebbberration, the bebbbholder, the tiny crebbb and the thrumebbb. No pink or
  missing texture, no shape on a wall. It is **not** a clean capture: the interface is drawn, with a mouse-over
  label for the ebbberration, and the goliebbb is not shown at twice a colonist's height for comparison. What it
  does not show: the corpses, the health tab, or anything in another language.
- **Where the evidence is:** `Tests/Pickle/Evidence/p1-english/`, ignored by git, 0.56 MB after cutting: the four
  text files, `evidence-complete.txt` and the minified capture. `report.html`, `messages.ndjson` and the two
  failure screenshots, which are the same image, were deleted: 30.8 MB, and they add nothing the four files do not
  carry.

## P2, minimal, French: request `20260924-165446-343-e6e4`

- **Result:** `exitReason: failed`, 14 scenarios, **7 passed, 2 failed, 5 skipped**, as expected: 9 to play and the
  same 5 skipped by requirement (`05`, `08` x4).
- **The two failures are the same two as in P1**, for the same reason: two lines about the test companion. No
  other error or warning in `Player.log` names a def, a texture, a duplicate or a patch of this mod, and nothing
  reads as a missing translation key.
- **What passing settled.** The five French scenarios of `07` passed: each label, description, tool, body part,
  life stage and meat label of the 104 reads as the French inventory says, in a game started in French. The
  descriptions differ between the two languages, so this could not have passed on English text.
- **No capture was owed.** Nothing in this pass is `@review`. What a French window looks like, and whether a long
  text clips, is still not asserted.
- **Evidence:** `Tests/Pickle/Evidence/p2-french/`, 0.13 MB after cutting from 22.5 MB: the four text files and
  `evidence-complete.txt`. The report, the message file and the two failure screenshots were deleted.

## The fix, and the request that checks it: `20260924-235620-812-a03b`

The two failures were the harness's. The steps that read the log now leave out lines that name the companion,
and the companion's dependency on this mod declares its Workshop item, which removes the first message. The
second, "did not load any content", stays and is excluded by name. The step DLL was rebuilt at 23:56 and
`Check-Steps.ps1` passes. **One request, two scenarios**, in English: the two that failed, and nothing else, since
a fix does not need the suite. The result is not known yet. The full P1 and P2 come again as final validation
once it is green.
