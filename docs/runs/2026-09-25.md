# 2026-09-25

The four final passes, submitted from `ff6b205`, one request each. Evidence cut to the four text files under
`Tests/Pickle/Evidence/<pass>/`; the reports, message files and captures were deleted (about 130 MB). The
game is RimWorld 1.6.4871 rev600 in the WSL, as on the 2026-09-24.

| Pass | Request | `exitReason` | Played | Passed | Failed | Skipped |
|---|---|---|---|---|---|---|
| P2, French | `20260925-131031-434-243f` | passed | 14 | **9** | 0 | 5 |
| P1, English | `20260925-131027-595-e99a` | failed | 20 | 9 | 6 | 5 |
| P4, A Dog Said 2 | `20260925-131034-449-dca3` | failed | 8 | 4 | 4 | 0 |
| P3, the original | `20260925-131037-870-add9` | failed | 1 | 0 | 1 | 0 |

## P2 is the first green pass

All nine scenarios that play passed, and the five that need another mod were skipped, as expected. The two log
scenarios that failed on 2026-09-24 pass: the fix to the harness holds in French too.

## P1: six map scenarios failed on an error that is not this mod's

The six scenarios that load `test-colony` (`02` x2, `03` x3, `04`) failed with "Accessing map pawns off main
thread - this is never allowed", about 7 to 13 s after each load. The three menu-only features passed. The same
steps had passed on 2026-09-24 at night, so nothing in this suite changed for them, and the Pickle-only
scenario `02` fails as well as the ones with local steps. EponaInstrumentsRenew's report of the same afternoon
shows the same message, and none of the other 156 recent reports do. Suspected cause, **not proven**: something
polls the runner's dashboard, `GET /state`, from another thread while a map is loaded. Three `pickle-queue.sh -w`
watchers and the dispatcher's own scripts were running, and two of them poll that endpoint every 10 to 30 s. The
finding was passed to the dispatcher, no process was touched, and one map scenario is requested alone to see
whether it reproduces (`20260925-153756-781-c5c6`).

## P4: it staged the wrong order, which showed why `loadBefore` exists

All four scenarios of `08` failed. The load order printed in the failure is Harmony, the expansions, RimLogging,
Pickle, **A Dog Said 2, then this mod**. The staging script activates the mods a pass map names before the mod
under test and does not sort by `loadBefore`. With the order reversed, A Dog Said 2 copied its category lists into
its recipes first, and the species were added to lists nobody read: `Ebbb` had 26 recipes and not the peg leg, and
so did `Crebbb` and `Beee`. That is the failure `About.xml`'s `loadBefore` prevents, seen by accident, and it
confirms that the patch does nothing when it runs too late. The pass map now names this mod's own folder first,
which the staging treats as an overlay of the mod under test, so the pass stages the declared order; the request
is `20260925-153755-054-9822`. What the game's own sorting does with `loadBefore` is the game's and is not tested.

The four `01` scenarios in that pass passed: with A Dog Said 2 loaded nothing was logged naming a def of this mod.

## P3: the first step timed out

`Given the main menu is open` timed out after 5 s, before any assertion. In a plain pass the same step took 4.4 s;
with the original mod beside this one its extra defs and load errors pushed it to 5.2 s. Nothing about the
incompatibility was asserted. **What the log does show is that the original is broken on 1.6 for the reason this
port fixes**: nine errors of the form "0.7 doesn't correspond to any field in type RaceProperties", its
`wildness` values. No "Adding duplicate" line is in that log, but the game may not have reached the point that
logs it before the step failed, so this says nothing yet about the duplicates. Every scenario that starts from the
menu now begins with a local step that waits up to two minutes for the game to finish starting, and the request is
`20260925-153753-095-0959`.

## Requests waiting

Three small ones, from `e81ba40`: P3 fix (`05`, 1 scenario), P4 fix (`08`, 4), and one map scenario from P1. The
final full passes come again after them.

## P4 fix: green, in a running game

Request `20260925-153755-054-9822`, `exitReason: passed`, 4 of 4, no retry, with this mod staged before A Dog Said 2
by the pass map. **The native support works.** Through `ThingDef.AllRecipes`, which is what the health tab reads:
the ebbb and the bebbbholder are offered the peg leg and not the simple prosthetic leg or the bionic leg; the
crebbb, the ebbberration, the drebbbd and the goliebbb are offered the peg leg and the simple prosthetic leg and not
the bionic leg; the beee, the ebbbomination and the thrumebbb are offered all of them, the power claw included. The
same four scenarios failed on the earlier run for one reason, the order, so the pair of runs shows both what
`loadBefore` does and that the categories come out as decided. No error or warning in `Player.log` names a patch,
a category recipe or a def of this mod. Evidence in `Tests/Pickle/Evidence/fix-p4/`, cut to the text files.

## The map scenario alone: green, so the error is intermittent

Request `20260925-153756-781-c5c6`, the P1 scenario "each species can be generated and stands on the map", alone:
`exitReason: passed`, 1 of 1, in 15 s, and no line about map pawns off the main thread in `Player.log`. The six
failures of the full P1 were therefore not deterministic and not caused by these steps, which are the same. The
dispatcher found no reader of the runner's dashboard during that pass beyond a watcher present in runs that did not
fail, and Epona had the same message on 2026-09-22, before that watcher existed. It is passed on to the Pickle
Tools session as an intermittent defect, with no stack trace of its origin: `Player.log` only holds the failure
echoes. The full P1 is requested again, once, as the final validation.

## The final passes, requested from the tree of `e81ba40`

P1 `20260925-165605-503-e147`, P2 `20260925-165607-245-a4d0` and P4 `20260925-165608-515-d05f`, all scenarios of
each pass. P2 and P4 are replayed because the companion changed since their earlier green: every menu scenario now
waits for the game to finish starting. P3 is a single scenario, so its retry `20260925-165238-110-2299`, after the
crash of the first try, is also its final pass if it is green.
