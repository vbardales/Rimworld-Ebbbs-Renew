# Protocols read

What was read, in which version, and whether it was worth reading, so that a document that moves is not
reread for nothing. The rule comes from the dispatcher's `WELCOME.md`, point 5: reread `AUDIT.md`,
`AGENTS.md` and `WELCOME.md` at the start of a session and after each compaction of the context, and write
down the version read. The version of a shared document is **the last commit that touched it**
(`git log -1 --format='%h %ad' -- <file>`), and `modified, uncommitted` when `git status` shows it changed.
Every document below was read on 2026-09-25, in full except three: `STYLE_RIMWORLD.md` (lines 1 to 150 and 170
to 210 of about 300, the rest being the engraved-text and count rules for images), `PickleTools/Docs/steps.md`
(the first 70 lines, then a search for the steps this suite could use) and `OPERATIONS.md` (all but the
credentials procedure, lines 109 to 163).

The mod repository was at `289ff2e` when this was written.

## Shared documents

| Document | Version read | Worth it for this mod | When it moves |
|---|---|---|---|
| `AGENTS.md` | monorepo `90d51374`, 2026-09-25 | **Needed.** Evidence policy (keep the latest report per scenario, delete the rest, list before deleting, one text line per run), the launcher archives to delete after use, the CI publishing rules. Short | Reread whole |
| `AUDIT.md` | monorepo `90d51374`, 2026-09-25 | **Needed.** The gates and the Pickle rules. What I took from it: gate 8 asks for Pickle tests **written** with their scope justified, gate 9 for green suites, no `@wip`, every `@requires` played, no manual test left; gate 11 and the fail-fast policy of 2026-09-25; the dispatcher paragraphs | Reread the gates 8 to 11, "Tests Pickle" and "Déposer un run". **Skip** the paragraphs on sound capture, the sleep freeze, Prepatcher and Concord, click windows, screenshots for publication and the translated-interface reading: none applies to a content mod with no UI |
| `PUBLISHING.md` | monorepo `90d51374`, 2026-09-25 | **Needed at `prepublished`, not before.** Taken from it: the description order, naming the real AI tools, thanking every integration that is named or exercised, the global thank-you registry, the `ATTRIBUTION.md` copies compared by hash, release notes that start with the version, GitHub topics and social preview | Reread "Mentions", "Licence", "Dépôt", "À chaque mise à jour" and the CI section. **Skip** the part from "Pièges d'outillage" on (junctions, parallel sessions in one repository, amend and reset, history repair): this repository is standalone, and I only keep "reread `git status` after the commit" |
| `TRANSLATIONS.md` | monorepo `90d51374`, 2026-09-25, file dated 2026-09-13, hash `3368579D01` | **Needed once**, when texts change: the inventory, DefInjected paths, the fields in `STATUS.md`. Already applied on 2026-09-13 | **Skip** while the hash is the same. Reread only if a text or a Def changes |
| `STYLE_RIMWORLD.md` | monorepo `90d51374`, 2026-09-25 | **Not useful.** It is about generating images and an icon is never generated by a session. Only the section "ModIcon : contrôle, pas génération" and the 268 px and 32 px checks matter, and only when auditing the images | **Skip** unless auditing the images |
| `scripts/SEARCHING.md` | monorepo `90d51374`, 2026-09-25, file dated 2026-09-17 | **Not useful now, but it explains something that happened here:** the session's own search tool times out at 20 s on the Workshop corpus, and `find` or a recursive `grep` over it hangs. Use `scripts/Search-Workshop.sh` for any search of the corpus | **Skip** unless a search of the corpus is needed |
| `PickleTools/README.md` | PickleTools `2b7b6d0`, 2026-09-25 | **Not useful.** The catalogue of shared step tools. This suite stages none of them: every step it needs is Pickle's own or its own | **Skip** unless a step needs a tool |
| `PickleTools/Headless/README.md` | PickleTools `b2712fc`, 2026-09-25 | **Partly.** The filter terms (an exclusion is not counted as skipped), the launcher's exit codes, the pass maps and `-DepMap`, and that the lock covers a download too | Reread "filter terms" and the exit-code table. **Skip** the rest (settings seeds, sleep, archives) |
| `PickleTools/Docs/steps.md` | not tracked, file dated 2026-09-25 17:43 | **Not useful.** 86 steps of shared tools, none used here. Pickle's own catalogue, which is the one that matters, is outside this repository, and I read it from the installed assemblies instead | **Skip** |
| `Rimworld-Release-Admin/docs/OPERATIONS.md` | Release-Admin `d403592`, 2026-09-25 | **Needed at `prepublished`, not before.** The publish workflow for a mod already on the Workshop, its dry-run, where the description and the change note come from | **Skip** until `prepublished` |
| `Rimworld-Ticket-Dispatcher/docs/WELCOME.md` | dispatcher `79668cc`, 2026-09-25 | **Needed.** One request per pass, the filter terms, `-DepMap`, no watcher, the tree frozen until `RUN_DONE`, the SHA in the label, how to delete evidence | Reread whole |
| `Rimworld-Ticket-Dispatcher/docs/SUBMIT.md` | dispatcher `79668cc`, 2026-09-25 | **Needed** for the options and the exit codes of a request | Reread the option table and the codes |

Two documents are referenced by those above and were **not in the list, so not read in this pass**:
`MOD_SETTINGS.md` (monorepo `90d51374`; applied on 2026-09-13, the outcome is `settings_audit:
not_applicable`) and `WORKSHOP_COMMENTS.md` (modified and uncommitted at the time, last commit `aba3082f`).
I only searched the latter, for the two Workshop items this mod names, and neither is in it.

## This repository's own documents

| Document | State | Note |
|---|---|---|
| `STATUS.md`, `README.md`, `CHANGELOG.md`, `ATTRIBUTION.md`, `TESTING.md`, `docs/runs/`, `Tests/Pickle/`, `Mod/About/About.xml` | present, written and kept by this session | The status sheet is the record. `Mod/ATTRIBUTION.md` is a byte-identical copy of the root one, checked by hash |
| `LICENSE` | present at the root (2026-09-25); the copy in `Mod/` follows once the queued passes are done | MIT for the port's own work, with a scope clause that excludes Coolie's content, as the sibling ports do. My first reading, "no LICENSE because Coolie states none", confused the two: the audit forbids inventing a licence for the original's content, not licensing one's own work |
| `PUBLICATION.md` | **absent, needed at `prepublished`** | The page's images in order, the thank-you comments (the registry has neither Coolie's item 2817264755 nor A Dog Said 2, 3238353862), the dependencies and DLC, the answer to the adult-content boxes, and the Steam change note under `### <version>` |
| `BACKLOG.md`, `NOTES.md`, `BUGS.md` | absent | Nothing to put in them: open work is in `STATUS.md` |
| `docs/PROTOCOLS-READ.md` | this file | |

## What reading changed

- The obsolete showcase note `PROMPT_EBBBSRENEW.md` at the collection root was deleted: `PUBLISHING.md` says it
  goes once `Preview.png` and `ModIcon.png` exist, and both do.
- The GitHub topics (`rimworld`, `rimworld-mod`, `mod`) and a custom social preview are already set on this
  repository, so nothing is owed there.
- At `prepublished` the description has to name Codex as well as Claude among the AI tools, thank Pickle and
  RimLogging as development-only, thank SamBucher for A Dog Said 2 and Coolie for the original, and end on the
  GitHub link. It cannot be resent by an update: it is written by hand on the Steam page.
